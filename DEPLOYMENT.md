# نشر منصة إتقان

## حالة النسخة الحالية

ملفات Render موجودة في `render.yaml`، ويمكن ربطها بمستودع GitHub ليُنشأ رابط `onrender.com` عام.

**لا تنشر النسخة الحالية كنسخة دائمة للبيانات.** هي تستخدم SQLite محليًا. استضافة Render المجانية تمسح ملفات SQLite والصور المرفوعة عند إعادة التشغيل أو السكون، لذا يجب تحويل قاعدة البيانات إلى PostgreSQL في Supabase قبل النشر العام النهائي.

## ما نحتاجه لإطلاق دائم

1. مستودع GitHub خاص باسم `etqan-quiz-platform`.
2. مشروع Supabase جديد، ثم إضافة اتصال PostgreSQL إلى متغيرات Render (لا تضع كلمة مرور القاعدة في GitHub أو المحادثة).
3. تحويل طبقة قاعدة البيانات في `server.js` إلى PostgreSQL وتشغيل ملف الجداول الأولي.
4. في Render: New > Blueprint، اختر المستودع، ثم أضف `ADMIN_EMAIL` و`ADMIN_PASSWORD` كمتغيرات سرية.

## التحقق بعد النشر

- افتح رابط Render الذي ينتهي بـ `onrender.com`.
- افتح `/#/admin` وسجّل دخول الإدارة.
- أضف مادة وسؤالًا، ثم أعد فتح الموقع للتحقق أن البيانات بقيت محفوظة.

## ملاحظات الخطط المجانية

- Render Free قد يدخل في وضع السكون بعد 15 دقيقة بدون زيارات؛ أول زيارة بعدها قد تتأخر نحو دقيقة.
- Supabase Free قد يوقف المشروع قليل النشاط بعد أسبوع؛ يمكن إعادته من لوحة Supabase دون فقدان فوري للبيانات.

